<li class="menu-item">
    <a
        target="{{ $item->target }}"
        class="{{ $item->link_class }}"
        href="{{ $item->link }}"
    >
    <strong>{{ $item->name }}</strong>
    </a>
</li>
